#include <stdio.h>
#include <string.h>

char *abbreviate(const char *phrase){
    char * acro;
    size_t len = len(phrase);
    char letters[len];
    for (size_t i =0; i<len; i++){
      
    }
    return acro;
}
